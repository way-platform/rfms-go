edition = "2023";

package wayplatform.connect.scania.rfms.v1;

// Credentials for Scania rFMS OAuth2 client credentials authentication.
message Credentials {
  // OAuth2 client ID.
  string client_id = 1;
  // OAuth2 client secret.
  string client_secret = 2 [debug_redact = true];
}
