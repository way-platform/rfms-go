edition = "2023";

package wayplatform.connect.volvotrucks.rfms.v1;

// Credentials for Volvo Trucks rFMS HTTP Basic authentication.
message Credentials {
  // HTTP Basic username.
  string username = 1;
  // HTTP Basic password.
  string password = 2 [debug_redact = true];
}
